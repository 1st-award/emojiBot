변경 내용과 운영 안내

- `Main.py`의 `~` 메시지는 `Util/EmojiCommands.py`에서 해석·조회·전송합니다. 서버 이모지가 공용 이모지보다 우선합니다. `~랜덤`은 실제 파일과 서버를 함께 선택하며, 비어 있는 목록도 처리합니다. 일반 메시지의 접두사 명령 처리와 봇 화이트리스트는 유지합니다.
- `LOG_LEVEL`은 DEBUG, INFO(기본), WARNING, ERROR, CRITICAL 중 선택합니다. 콘솔과 `logs/bot.log`에 시간·레벨·모듈을 기록합니다. 파일은 5MB마다 교체하며 이전 파일 3개를 보관합니다. DEBUG는 조회·이미지 처리, INFO는 시작·데이터 변경, WARNING은 권한·입력 거부, ERROR는 예외 추적에 사용합니다. Discord의 별도 기본 핸들러는 비활성화해 중복 출력을 막습니다.
- SQLite는 작업마다 연결하고 반드시 닫습니다. 모든 연결에서 외래 키를 활성화하며, 쓰기 실패는 롤백합니다. 기존 테이블 구조와 `(guild, path, command)` 반환 순서는 유지합니다. DB가 없을 때 빈 파일을 자동 생성하지 않습니다.
- 사용자 입력은 SQL 매개변수로만 전달합니다. 부분 검색은 유지하지만 `%`, `_`, `!`를 이스케이프하며 정확히 일치하는 명령을 우선합니다. 삭제와 중복 확인은 정확한 일치로 처리합니다. 일괄 삽입은 대상 서버를 검증하고 전체를 한 트랜잭션으로 처리합니다.
- 등록 중 이미지 저장·변환에 실패하면 등록 행과 파일을 정리합니다. 서버 복사는 기존 길드 행과 점수 데이터를 삭제하지 않고 이모지 매핑만 교체합니다. 서로 다른 내용의 파일 충돌은 거부합니다. 파일 시스템과 DB를 하나의 원자적 트랜잭션으로 묶지는 않으므로 복사 중 실패하면 참조되지 않는 복사 파일이 남을 수 있습니다.
- 확장 로드·언로드·리로드와 서버 간 복사는 봇 소유자로 제한했습니다. 기존 복사 인자의 방향은 유지합니다: `to_guild_id`가 원본, `from_guild_id`가 대상입니다.
- APEX 키는 코드에서 제거했습니다. 해당 기능을 사용하려면 `APEX_API_TOKEN`을 설정해야 합니다. 기존 소스에 들어 있던 키는 발급처에서 교체하는 것이 좋습니다. `.env.example`에는 실제 비밀 값이 없습니다.
- 이미지 경로의 상위 디렉토리 접근을 차단했고, GIF 크기 분기·프레임 처리와 이미지 분류 입력 배열을 정리했습니다. 이미지 분류 기능은 별도 Keras 모델과 numpy/matplotlib/Keras 의존성이 필요합니다.

검증

`python -m unittest discover -s tests -v`

임시 SQLite DB로 SQL 삽입 문자열, 와일드카드 리터럴, 정확한 삭제, 중복, 서버 격리, 외래 키·연쇄 삭제, 일괄 삽입·교체 롤백, 스레드 조회를 검증합니다. 메시지 접두사·파일 닫기, 빈 랜덤 목록, 이미지 경로·MIME, GIF 크기·반복·프레임 시간도 검증합니다. 운영 DB는 스키마만 읽었으며 수정하지 않았습니다.

사용 가능한 시스템 Python에서 테스트 및 모든 Cog 로딩을 확인했습니다. 기존 `.venv`는 Python 실행 파일 경로가 유효하지 않아 사용하지 않았습니다. Discord 실접속, 외부 APEX API, Keras 모델 추론은 실행하지 않았습니다. 운영 환경에서는 등록·삭제·복사 및 권한 오류 응답을 확인해야 합니다.

`Old_Cogs`, `dcon_upater.py`, 실제 파일인 `dcon_updater.py`는 변경하지 않았습니다.

SQLite 검토 기준: https://docs.python.org/3/library/sqlite3.html 및 https://www.sqlite.org/foreignkeys.html
